# Hi there!👋

In this GitHub repository, I share code on microsoft dynamic 365 with pyth0n.

# Microsft dynamic 365 with Python 


## For this repository, the prerequisites are:

Before diving into this repository, ensure you have the following installed:

1.  **Python:** Which you can download from the [official Python website](https://www.python.org/downloads/).
2.  **msal:** Install the library using pip in your bash terminal or your directory:
    ```bash
    pip install msal
    ```
3.  **Requests:** Install the library using pip in your bash terminal or your directory:
    ```bash
    pip install requests
    ```
4.  **Requireents txt:** To Install other libraries at once within in your bash terminal or your directory:
    ```bash
    pip install -r requirements.txt
    ```

## How to Run Examples

To execute the Python scripts in this repository:

1.  Navigate to the directory containing the .py files in your terminal.
2.  Run the desired script using the Python interpreter:
    ```bash
    python filename.py
    ```
    (Replace `filename.py` with the actual name of the script).



